{
  "name": "telegram-group-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npx vite build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
